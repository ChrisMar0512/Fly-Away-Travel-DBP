version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fly-away-travel-api
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./:/host
    restart: unless-stopped
